export const CONFIG = {
  // Se true, ativa o Modo Feira simplificado
  FAIR_MODE: false,
  
  // Chave de API global do Gemini (opcional). 
  // Se preenchida, todos os participantes usarão esta chave.
  // Se vazia, os participantes usarão o simulador offline por padrão.
  DEFAULT_API_KEY: '',
};
